services:
  - type: web
    name: matchmypet-api
    runtime: node
    rootDir: codigo
    buildCommand: npm install
    startCommand: node api-server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
